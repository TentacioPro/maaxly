---
applyTo: '**'
---
**Objective:** Recreate [Target Component/Page, e.g., the Student Dashboard] in my project.

**Source of Truth:** Use the code and design patterns from the directory `/_golden-repo/` as the primary reference. Pay close attention to its component structure, layout, and shadcn/ui usage.

**Key Reference File(s):** For this task, specifically analyze `/_golden-repo/src/app/dashboard/page.tsx` and its related components.

**Language Constraint:** The reference code is in TypeScript (TSX). You MUST generate the new code in plain JavaScript (JSX). Do not include any TypeScript types, interfaces, or syntax.

**Task:**
1.  Analyze the reference file(s) to understand the layout, data display, and components used (e.g., `Card`, `Table`, `StatCard`).
2.  Write the JSX code for my `[Your File Name, e.g., src/pages/StudentDashboard.jsx]` file.
3.  Ensure the new JSX code accurately reflects the design and structure of the reference, using the same shadcn/ui components and class names.